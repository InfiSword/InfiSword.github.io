# InfiSword.github.io

Static portfolio site for GitHub Pages.

## Build

```bash
npm run build
```

The build script converts `content/home.html` and `content/projects/*.md` into static HTML pages. Jekyll is not required.
